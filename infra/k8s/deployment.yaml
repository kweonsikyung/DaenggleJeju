apiVersion: apps/v1
kind: Deployment
metadata:
  name: daengglejeju-web
  labels:
    app: daengglejeju-web
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: daengglejeju-web
  template:
    metadata:
      labels:
        app: daengglejeju-web
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: web
          image: ghcr.io/kweonsikyung/daengglejeju:sha-c661f67308c4114fd03f05e3fbf4a8570f0fd61d
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
            - name: HOSTNAME
              value: "0.0.0.0"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
